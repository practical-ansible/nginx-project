---
# @meta description: > 
# Use this to avoid Burnout Syndrome when deploying your Docker wrapped application 
# to nginx.
# @end
galaxy_info:
  author: Pavel Žák
  description: Deploy your Docker project to Nginx with minimal config
  license: MIT
  min_ansible_version: 2.8
  platforms:
   - name: Ubuntu
     versions:
     - all

  galaxy_tags:
    - nginx
    - deploy
    - hosting
    - letsencrypt
    - certbot
    - http
    - https
    - docker
